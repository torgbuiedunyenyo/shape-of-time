# D2 blind-read scorecard (template)

For the human readers of one uninterrupted 8–14-folio baseline run (PLAN §D2). Read the folios in
order from the run archive (`folio-NN.result.json` → `prose`), blind: no prompts, no briefs, no
model output metadata. A model score cannot override a human failure recorded here.

Run archive root: `<fill in>` · Run date: `<fill in>` · Reader: `<fill in>`

## Per folio

Copy this block once per folio.

```
Folio N
- What physically happened (one or two sentences, in your own words):
- What each actor on the page wanted:
- Why the page changed (what made the situation different at the end):
- Missing orientation? (moments you did not know where/when/who) —
- Forced exposition? (people explaining familiar facts for the reader) —
- Uncashable abstraction? (claims or images that never touch a particular) —
- Did you want to turn the page? (yes / no / barely) —
- Did any phrase make you want to open a door into another book? Which? —
```

## Whole run

- Are at least eight consecutive folios concrete, causally intelligible, canon-compatible,
  paraphrasable, and worth continuing? (the D2 green bar): YES / NO
- The single most repeated named failure, if any:
- One variable to change for the rerun (one prompt/beat variable only; if the repeated failure is
  placeless abstraction, above-the-scene reporting, or explanatory summary, the first tested
  change is the controlled A/B that adds the complete adapted craft-example block and changes
  nothing else):
